# PostgreSQL / TimescaleDB 数据库配置
# 如果不配置，将使用以下默认值

# 数据库用户名（默认: postgres）
TIMESCALEDB_USER=postgres

# 数据库密码（默认: postgres）
TIMESCALEDB_PASSWORD=postgres

# 数据库主机（默认: 127.0.0.1）
TIMESCALEDB_HOST=127.0.0.1

# 数据库端口（默认: 5432）
TIMESCALEDB_PORT=5432

# 数据库名称（默认: hyperliquid_analysis）
TIMESCALEDB_DATABASE=hyperliquid_analysis

# 增量更新功能使用指南

## 📚 概述

增量更新功能允许你只获取最新的交易记录，而不是每次都重新获取全部历史数据，大幅提升性能和效率。

---

## 🎯 核心特性

### ✅ 优势

- **性能提升**: 10-50倍速度提升（取决于历史数据量）
- **API友好**: 减少API调用次数，降低限流风险
- **带宽节省**: 只传输新增数据
- **自动智能**: 首次自动全量，后续自动增量
- **向后兼容**: 保留全量更新模式

### 📊 效果对比

```python
# 场景：地址已有 5000 条历史交易，新增 100 条

# ❌ 全量更新（旧方式）
# - API调用: 3次分页（5100/2000 = 2.55）
# - 数据传输: 5100条
# - ⏱️ 耗时: ~8秒

# ✅ 增量更新（新方式）
# - API调用: 1次（100条）
# - 数据传输: 100条
# - ⏱️ 耗时: ~0.5秒
# - 性能提升: 16倍 🚀
```

---

## 🚀 快速开始

### 基础使用

```python
import asyncio
from address_analyzer.data_store import get_store
from address_analyzer.api_client import HyperliquidAPIClient

async def main():
    # 初始化
    store = get_store()
    await store.connect()

    client = HyperliquidAPIClient(store)

    # 增量更新（推荐）
    data = await client.fetch_address_data(
        address="0x563c175e6f11582f65d6d9e360a618699baec1b8",
        incremental=True  # 默认值，可省略
    )

    print(f"获取到 {len(data['fills'])} 条新记录")

    await store.close()

asyncio.run(main())
```

---

## 📖 API 参考

### 1. `fetch_address_data()`

获取地址的完整数据（支持增量更新）

```python
async def fetch_address_data(
    address: str,
    save_to_db: bool = True,
    incremental: bool = True  # 🆕 新增参数
) -> Dict[str, Any]
```

**参数说明**:

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `address` | `str` | 必填 | 用户地址 |
| `save_to_db` | `bool` | `True` | 是否保存到数据库 |
| `incremental` | `bool` | `True` | 是否启用增量更新 |

**返回值**:

```python
{
    'address': str,           # 地址
    'fills': List[Dict],      # 交易记录（增量模式返回新记录）
    'state': Dict,            # 账户状态
    'spot_state': Dict,       # Spot账户状态
    'funding': List[Dict],    # 资金费率历史
    'ledger': List[Dict]      # 出入金记录
}
```

### 2. `get_user_fills()`

获取用户交易历史（支持增量和分页）

```python
async def get_user_fills(
    address: str,
    use_cache: bool = True,
    incremental: bool = True  # 🆕 新增参数
) -> List[Dict]
```

**参数说明**:

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `address` | `str` | 必填 | 用户地址 |
| `use_cache` | `bool` | `True` | 是否使用缓存 |
| `incremental` | `bool` | `True` | 是否启用增量更新 |

**工作逻辑**:

1. **增量模式** (`incremental=True`):
   - 查询数据库最新交易时间
   - 如果有记录 → 从最新时间+1ms开始获取
   - 如果无记录 → 自动执行全量获取（从0开始）
   - 新数据自动保存到数据库

2. **全量模式** (`incremental=False`):
   - 从0开始获取所有历史记录
   - 使用缓存机制
   - 手动保存到数据库

### 3. `get_latest_fill_time()`

获取地址最新的交易时间戳（新增方法）

```python
async def get_latest_fill_time(address: str) -> Optional[int]
```

**参数说明**:

| 参数 | 类型 | 说明 |
|------|------|------|
| `address` | `str` | 用户地址 |

**返回值**:

- 成功: 返回最新交易的时间戳（毫秒）
- 无记录: 返回 `None`

---

## 💡 使用场景

### 场景1: 首次获取地址数据

```python
# 首次获取（数据库无记录）
data = await client.fetch_address_data(
    address="0xabc...",
    incremental=True  # 自动检测到无记录，执行全量获取
)

# 日志输出:
# [0xabc...] 首次获取: 从 0 开始
# [0xabc...] 第 1 页: 2000 条记录
# [0xabc...] 第 2 页: 1500 条记录
# [0xabc...] 共获取 3500 条新记录
```

### 场景2: 定时增量更新

```python
import asyncio
from datetime import datetime

async def incremental_update_loop(addresses, interval_minutes=5):
    """定时增量更新多个地址"""

    store = get_store()
    await store.connect()
    client = HyperliquidAPIClient(store)

    while True:
        logger.info(f"[{datetime.now()}] 开始增量更新")

        for address in addresses:
            try:
                # 增量更新
                data = await client.fetch_address_data(
                    address,
                    incremental=True  # 🎯 只获取新记录
                )

                new_fills = len(data['fills'])
                logger.info(f"[{address}] 新增 {new_fills} 条记录")

            except Exception as e:
                logger.error(f"[{address}] 更新失败: {e}")

        # 等待下次更新
        await asyncio.sleep(interval_minutes * 60)

# 每5分钟更新一次
asyncio.run(incremental_update_loop(
    addresses=["0xabc...", "0xdef..."],
    interval_minutes=5
))
```

### 场景3: 强制全量刷新

```python
# 需要重建数据或修复数据时使用
data = await client.fetch_address_data(
    address="0xabc...",
    incremental=False  # 强制全量更新
)

# 日志输出:
# [0xabc...] 全量更新模式
# [0xabc...] 第 1 页: 2000 条记录
# [0xabc...] 共获取 3550 条记录
```

### 场景4: 检查最新交易时间

```python
# 查询数据库最新交易时间
latest_time = await store.get_latest_fill_time("0xabc...")

if latest_time:
    from datetime import datetime
    dt = datetime.fromtimestamp(latest_time / 1000)
    print(f"最新交易时间: {dt}")
else:
    print("数据库无记录")
```

---

## 🔍 工作原理

### 增量更新流程图

```
┌─────────────────────────────────────────┐
│ fetch_address_data(incremental=True)   │
└────────────┬────────────────────────────┘
             │
             ↓
┌────────────────────────────────────────┐
│ get_user_fills(incremental=True)       │
└────────────┬───────────────────────────┘
             │
             ↓
┌────────────────────────────────────────┐
│ 查询数据库最新时间                       │
│ get_latest_fill_time(address)          │
└────────────┬───────────────────────────┘
             │
      ┌──────┴──────┐
      │             │
   有记录         无记录
      │             │
      ↓             ↓
 从最新时间+1    从0开始
  开始获取       (全量获取)
      │             │
      └──────┬──────┘
             │
             ↓
┌────────────────────────────────────────┐
│ API分页获取数据                         │
│ user_fills_by_time(start_time)         │
└────────────┬───────────────────────────┘
             │
             ↓
┌────────────────────────────────────────┐
│ 自动保存到数据库                        │
│ save_fills(address, fills)             │
└────────────┬───────────────────────────┘
             │
             ↓
┌────────────────────────────────────────┐
│ 返回新记录                              │
└────────────────────────────────────────┘
```

### 数据库查询优化

增量更新依赖以下索引:

```sql
-- 已存在的索引（支持快速查询最新时间）
CREATE INDEX idx_fills_address_time ON fills(address, time DESC);

-- 查询最新时间的SQL（<10ms）
SELECT EXTRACT(EPOCH FROM MAX(time)) * 1000 AS latest_time_ms
FROM fills
WHERE address = '0xabc...';
```

---

## ⚙️ 配置建议

### 定时更新间隔

根据地址活跃度选择合适的更新间隔:

| 地址类型 | 交易频率 | 推荐间隔 |
|----------|----------|----------|
| 高频交易者 | >100笔/天 | 1-5分钟 |
| 中频交易者 | 10-100笔/天 | 10-30分钟 |
| 低频交易者 | <10笔/天 | 1-6小时 |
| 观察地址 | 不定期 | 12-24小时 |

### 并发控制

```python
# 批量增量更新多个地址
async def batch_incremental_update(addresses):
    """批量增量更新（带并发控制）"""

    client = HyperliquidAPIClient(
        store,
        max_concurrent=5,      # 最大并发数
        rate_limit=10.0        # 10请求/秒
    )

    tasks = [
        client.fetch_address_data(addr, incremental=True)
        for addr in addresses
    ]

    results = await asyncio.gather(*tasks, return_exceptions=True)

    # 统计结果
    success = sum(1 for r in results if not isinstance(r, Exception))
    print(f"成功更新: {success}/{len(addresses)}")
```

---

## 🧪 测试

运行测试脚本:

```bash
python test_incremental_update.py
```

**测试内容**:

1. ✅ 首次获取（自动全量）
2. ✅ 增量更新（仅新数据）
3. ✅ 强制全量刷新
4. ✅ API统计信息
5. ✅ 性能对比分析

---

## ⚠️ 注意事项

### 1. 数据一致性

- 增量更新依赖数据库中已有记录的时间戳
- 如果手动修改或删除数据库记录，可能导致数据不一致
- **建议**: 数据修复时使用 `incremental=False` 强制全量刷新

### 2. 时间戳精度

- API使用毫秒级时间戳
- 增量更新使用 `最新时间 + 1ms` 避免重复
- **注意**: 理论上可能遗漏同一毫秒内的多笔交易（概率极低）

### 3. API限制

- Hyperliquid API单次最多返回2000条记录
- 分页机制自动处理，无需担心
- **建议**: 定时增量更新避免单次获取量过大

### 4. 缓存策略

- 增量模式不使用缓存（实时性优先）
- 全量模式使用24小时缓存
- **建议**: 生产环境使用增量模式

---

## 🔧 故障排查

### 问题1: 增量更新返回0条记录

**原因**: 地址最近没有新交易

**解决**: 正常现象，无需处理

### 问题2: 增量更新遗漏数据

**可能原因**:
- 数据库记录被手动删除
- 时间戳不准确

**解决方案**:
```python
# 强制全量刷新重建数据
data = await client.fetch_address_data(
    address="0xabc...",
    incremental=False  # 全量模式
)
```

### 问题3: 性能没有提升

**检查项**:
1. 确认 `incremental=True`
2. 确认数据库有历史记录
3. 检查API调用日志

**调试代码**:
```python
# 查看数据库记录
latest_time = await store.get_latest_fill_time(address)
print(f"数据库最新时间: {latest_time}")

# 检查API统计
stats = client.get_stats()
print(f"API调用次数: {stats['total_requests']}")
```

---

## 📈 性能监控

### 建议监控指标

```python
# 定时任务中添加监控
async def monitored_update(address):
    import time

    start_time = time.time()

    # 增量更新
    data = await client.fetch_address_data(address, incremental=True)

    elapsed = time.time() - start_time
    new_fills = len(data['fills'])

    # 记录指标
    logger.info(f"[{address}] 耗时: {elapsed:.2f}s, 新记录: {new_fills} 条")

    # 异常检测
    if elapsed > 5.0:
        logger.warning(f"[{address}] 更新耗时过长: {elapsed:.2f}s")

    if new_fills > 1000:
        logger.warning(f"[{address}] 新记录过多: {new_fills} 条")
```

---

## 🎓 最佳实践

1. ✅ **默认使用增量模式**: 除非需要全量刷新
2. ✅ **合理设置更新间隔**: 根据地址活跃度调整
3. ✅ **监控更新性能**: 及时发现异常
4. ✅ **定期全量校验**: 每周/每月执行一次全量刷新确保数据完整
5. ✅ **错误处理**: 增量失败时自动回退到全量模式

---

## 📚 相关文档

- [API客户端文档](../address_analyzer/api_client.py)
- [数据存储文档](../address_analyzer/data_store.py)
- [数据库设计](DATABASE_SCHEMA_DESIGN.md)

---

**最后更新**: 2024-01
**版本**: 1.0.0
